import LoadingScreen from "../../common/LoadingScreen";

const LoadingSpinner = ({ message, height, size }) => {
  return <LoadingScreen message={message} height={height} size={size} />;
};

export default LoadingSpinner;
